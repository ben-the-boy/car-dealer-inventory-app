<!doctype html>
<html>
  <head>
    <title>AutoDealerInventory</title>
  </head>
  <% if flash.has?(:message) %>
    <%= flash[:message] %>
  <% end %><br>
  <h1>AutoDealerInventory</h1>
  <% if logged_in? %>
    <b><%= current_user.username %></b> <a href="/logout">Logout</a><br>
    <br>
    <a href="/vehicles">Home</a> <a href="/vehicles/new">Add New Vehicle</a><br>
    <br>
  <% else %>
    <a href="/login">Login</a> or <a href="/signup">Sign Up</a>
  <% end %>
  <body>
    <%= yield %>
  </body>
</html>
